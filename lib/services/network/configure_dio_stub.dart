import 'package:dio/dio.dart';

/// Configures platform-specific Dio settings for Mobile & Desktop (Native IO).
void configureDioPlatform(Dio dio) {
  // Mobile / Desktop native IO configuration.
  // Standard HTTPS certificate validation is preserved by default.
}
